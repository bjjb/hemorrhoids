source 'https://rubygems.org'

# Specify your gem's dependencies in hemorrhoids.gemspec
gemspec

if ENV['AR_VERSION']
  gem 'activerecord', "~> #{ENV['AR_VERSION']}"
else
  gem 'activerecord'
end

gem "rake"
gem "minitest"
gem 'guard-minitest'
gem 'activerecord'
gem 'sinatra'
